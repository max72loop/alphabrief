__pycache__/
*.pyc
*.pyo
data/cache/
data/*.db
data/*.bak
.env

# MemPalace / graphify artefacts
entities.json
mempalace.yaml

# Local agent prompt drafts (committed copies are fine, drafts are not)
prompt_agent.txt.local

# Sorties de backtest (artefacts régénérables)
data/backtests/

# Base miroir : artefacts locaux
db/mirror/*.local.sql
